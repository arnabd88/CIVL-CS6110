#include <assert.h>

int N = 0;

typedef enum color_t{
  WHITE,
  RED,
  GREEN
} color_t;

int $sef$0 = N;
N = N + 1;

typedef struct car_t{
    int year;
    int  log[$sef$0];
} car_t;

int main()
{
  int k = 2;
  car_t myCar;
  assert(k == GREEN);
  {
    int i = 0;
    for(; i < 3; i = i + 1)
      myCar.log[i] = i;
  }
}
