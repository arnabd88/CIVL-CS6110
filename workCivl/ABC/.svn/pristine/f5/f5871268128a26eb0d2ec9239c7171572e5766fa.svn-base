#include <assert.h>

int N = 0;

typedef enum color_t {
WHITE,
RED,
GREEN
}color_t;

typedef struct car_t{
int year;
int log[N++];
}car_t;

int main(){
  int k = 2;
  car_t myCar;
  
  assert(k == GREEN);
  for(int i = 0; i < 3; i++)
    myCar.log[i] = i;
}
