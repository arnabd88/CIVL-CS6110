package edu.udel.cis.vsl.abc.ast.type.IF;

/**
 * An unqualified object type is an object type without any qualifier (const,
 * volatile, etc.). This is a marker interface; it does not provide any methods.
 * It is used to create a sub-type of {@link ObjectType}.
 * 
 * @author siegel
 * 
 */
public interface UnqualifiedObjectType extends ObjectType {

}
