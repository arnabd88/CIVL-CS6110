/**
 * Module <strong>config</strong> defines a configuration of ABC
 * which specifies things such as the language be parsed, bounds on
 * integer types, and so on.
 */
package edu.udel.cis.vsl.abc.config.IF;

