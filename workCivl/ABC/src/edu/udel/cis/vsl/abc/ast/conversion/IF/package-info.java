/**
 * <p>Submodule <strong>ast.conversion</strong> provides classes for representing
 * to C's "implicit conversions".  These include numeric conversions which convert,
 * for example, a <code>float</code> to a <code>double</code>, the conversion of
 * a an array to a pointer to element 0 of the array, the conversion of a function name
 * to a pointer to that function, and so on.</p>
 */
package edu.udel.cis.vsl.abc.ast.conversion.IF;

