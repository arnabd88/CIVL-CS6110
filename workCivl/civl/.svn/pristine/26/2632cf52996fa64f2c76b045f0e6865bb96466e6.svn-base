package edu.udel.cis.vsl.civl.model.IF.expression;

/**
 * A wait guard expression is a sudo guard expression for a wait statement.
 * 
 * @author Manchun Zheng (zmanchun)
 * 
 */
public interface WaitGuardExpression extends Expression {
	/**
	 * The process that the wait statement is waiting for.
	 * 
	 * @return
	 */
	Expression joinedProcess();
}
